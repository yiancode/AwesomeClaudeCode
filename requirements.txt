# AwesomeClaudeCode 依赖 / Dependencies

# 必需 / Required
pyyaml>=6.0
requests>=2.31.0
python-dotenv>=1.0.0

# 可选 / Optional (for GitHub metadata extraction and crawling)
PyGithub>=2.1.1
feedparser>=6.0.10  # RSS feed parsing

# 测试 / Testing
pytest>=7.0.0
pytest-cov>=4.0.0
